mutation Edit($email: String!, $password: String!) {
  login(email: $email, password: $password) {
    accessToken
    user {
      id
      email
      username
      firstName
      familyName
      familyId
      prefersDarkMode
      userType
    }
  }
}